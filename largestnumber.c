#include<stdio.h>
int main()
{
    int a,b;
    printf("enter the value of a and b:");
    scanf("%d%d",&a,&b);
    if(a>b)
    printf("%d is a largest number\n",a);
    else
    printf("%d is a largest number\n",b);
}
